version: "1"

database:
  migrations:
    - schema.sql

queries:
  - "book.sql"
  - "author.sql"
  - "queries.sql"
codegen:
  language: python # typescript
  driver: psycopg
  out: ./queries

  types:
    pg_catalog.uuid:
      annotation: str
  enums:
    - "genre"
  options:
    package: queries
