version: "1"

database:
  migrations:
    - schema.sql

queries:
  - "book.sql"
  - "author.sql"
  - "queries.sql"
codegen:
  target: typescript:postgres
  out: ./queries

  types:
    pg_catalog.uuid:
      annotation: str
  enums:
    - "genre"
  options:
    package: queries
