version: "1"

database:
  # migrations:
  #   - schema.sql
  url: $DB_URL
  # pglite:
  #   extensions:
  #     pg_trgm: "@electric-sql/pglite/contrib/pg_trgm"
queries:
  - "book.sql"
  - "author.sql"
  - "queries.sql"
codegen:
  target: python:asyncpg
  out: ./queries

  types:
    pg_catalog.uuid:
      name: str

  options:
    package: queries
